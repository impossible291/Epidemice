<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.testgradution.testgradution.mapper.AdminMapper">
    <resultMap id="BaseResultMap" type="com.testgradution.testgradution.domain.Admin" >
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="password"  jdbcType="VARCHAR" property="password"/>
    </resultMap>

    <select id="verifyPassword" resultType="java.lang.Integer">
        select count(*) from admin where name=#{0} AND password=#{1}
    </select>

    <insert id="insertAdmin" parameterType="com.testgradution.testgradution.domain.Admin">
        insert  into gradutation.admin(name,password) values(#{name},#{password})
    </insert>

    <select id="selectAdmin" resultMap="BaseResultMap">
        select * from admin
    </select>

    <select id="selectName" resultType="java.lang.String">
        select  name from  gradutation.admin where name=#{name}
    </select>

    <select id="passIstrue" resultType="java.lang.Integer">
        select count(*) from gradutation.admin where name=#{username} AND password=#{password}
    </select>

</mapper>